import { createClasses } from "../../theme/create-classes.js";

// ----------------------------------------------------------------------

export const scrollbarClasses = {
  root: createClasses("scrollbar__root"),
};
